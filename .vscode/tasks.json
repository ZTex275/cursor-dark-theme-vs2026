{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Build VSIX",
      "type": "shell",
      "command": "npx @vscode/vsce package",
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "group": {
        "kind": "build",
        "isDefault": true
      },
      "problemMatcher": []
    }
  ]
}
